import styled from '@emotion/styled';

const Container = styled.div`
  width: 1200px;
  margin: 0 auto;
  position: relative;

  display: grid;
  grid-template-columns: 1fr;
  grid-gap: 16px;
  padding-bottom: 24px;
`;

export { Container };
